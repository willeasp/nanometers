NanoMeters.xcodeproj/
build/
DerivedData/
*.xcuserstate

# Local secrets — your real Google OAuth client id lives here (see Secrets.example.xcconfig). Never commit.
Secrets.xcconfig
# Generated by `xcodegen generate` from project.yml (so the build-substituted client id can't leak).
Sources/Info.plist
