import SwiftUI

// Temporary scaffold — remove each stub as the real screen lands (Tasks 9, 10).
// These exist only to keep the build green before the real screens arrive.
// LibraryScreen stub removed (Task 7). PlaylistsScreen stub removed (Task 8).
// PlaylistDetailScreen stub removed (Task 9).

struct SearchScreen: View {
    var body: some View { Text("Search") }
}
